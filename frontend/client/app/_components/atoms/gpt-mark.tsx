import React from "react"

interface GPTMarkProps {}

const GPTMark = (props: GPTMarkProps) => {
  const {} = props
  return <div className="text-mElement5 rounded-full border border-gray-50 px-6 py-4 text-gray-50">GPT 3.5</div>
}

export default GPTMark
